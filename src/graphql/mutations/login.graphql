mutation Login($username: String!, $password: String!){
  login(username: $username, password: $password){
    __typename
    ...on User{
      id
      name
      username
    }
    ...on FieldErrors{
      errors{
        message
        field
      }
    }
  }
}
